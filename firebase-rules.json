{
  "rules": {
    "bugs": {
      ".read": true,
      ".write": true,
      "$bugId": {
        ".validate": "newData.hasChildren(['title', 'type', 'status']) || !newData.exists()"
      }
    },
    "presence": {
      ".read": true,
      ".write": true
    }
  }
}
